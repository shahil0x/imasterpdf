let
    mindate=List.Min(SalesData[OrderDate]),
    maxdate=Date.From(DateTime.LocalNow()),
    datelist={Number.From(mindate)..Number.From(maxdate)},
    calender=Table.FromList(datelist,Splitter.SplitByNothing(),{"Date"}),
    #"Changed Type" = Table.TransformColumnTypes(calender,{{"Date", type date}})
in
    #"Changed Type"